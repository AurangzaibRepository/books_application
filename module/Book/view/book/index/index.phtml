<?= $this->headTitle('Books'); ?>

<h4>Books</h4>
<br />

<div class="d-flex justify-content-end">
    <a href="<?= $this->url('book', ['action' => 'add']) ?>"
        class="btn btn-primary">Add</a>
</div>

<div class="row">
    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th>No</th>
                    <th>Title</th>
                    <th>Description</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($bookList as $book): ?>
                <tr>
                    <td>1</td>
                    <td><?= $this->escapeHtml($book->getTitle()) ?>
                    </td>
                    <td><?= $this->escapeHtml($book->getDescription()) ?>
                    </td>
                    <td>
                        <a href="" class="btn btn-primary">Edit</a>
                        <a href="" class="btn btn-secondary">Delete</a>
                    </td>
                </tr>
                <?php endforeach ?>
            </tbody>
        </table>
    </div>
</div>